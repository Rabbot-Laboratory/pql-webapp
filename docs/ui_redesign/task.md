- [x] 1. **テーマ設定の変更**
  - `main.ts` を修正し、PrimeVueのテーマ設定でダークモードを強制(`darkModeSelector: '.app-dark'`)し、ルートにクラス適用。
- [x] 2. **カラースキームとUIシェイプの差し替え (`styles.css`)**
  - `--console-bg`, `--console-panel` などの変数をディープブラックおよび透明度の高いダークグレー基調に変更。
  - アクセントカラーをネオンシアン (`#66fcf1`) に設定。
  - 背景のグラデーションをテック感のあるシャープなトーンに変更（放射状シアン＋黒グラデーション）。
  - `border-radius` を全体的に削り、四角く無骨な印象に変更（xl:10px, md:4px）。
- [x] 3. **RABBOTロゴの配置と白抜き化**
  - ロゴファイルを `src/assets/logo.png` に配置。
  - `StatusToolbar.vue` のタイトル部分を画像(`@/assets/logo.png`)に置き換え。
  - CSSフィルタ (`filter: invert(1) brightness(1.2)`) を使い、黒背景でパキッと光るようにロゴを白抜き化。
- [x] 4. **ブラウザ確認と最終調整**
  - （完了）`RobotModelViewport.vue` の背景色も透過させ、ライティングもダークサイバー調のシアンベースに変更済み。ビルドテストクリアを確認。
